{% extends 'base.html.twig' %}

{% block body %}
    <div class=" container mt-3">
        <h1>Espace membre privé !</h1>
        <a href="{{ path('app_logout') }}">Me déconnecter</a>
        <a href="{{ path('app_account_modify_pwd') }}"> Modifier votre mot de passe</a>
    </div>

{% endblock %}
